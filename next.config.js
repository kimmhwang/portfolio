/** @type {import('next').NextConfig} */
const nextConfig = {
  // For Vercel: no changes needed, deploy as-is.
  // For GitHub Pages: uncomment the line below:
  // output: 'export',

  // If using a custom domain, no basePath needed.
  // If hosting at github.com/username/repo, set:
  // basePath: '/repo-name',
};

module.exports = nextConfig;
